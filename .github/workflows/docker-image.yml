name: Docker Image CD

on:
  workflow_dispatch:
 
env:
  IMAGE_NAME: webapi


jobs:

  release:

    runs-on: ubuntu-latest

    steps:
    
    
        
    - name: Checkout code
      uses: actions/checkout@v2
      
      
    
        
    #- name: build image
      #run: docker build -f Dockerfile  -t webapi:latest .
      
    - name: Login to Docker hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
        
    - name: Build and push
      uses: docker/build-push-action@v2
      with:
          context: .
          push: true
          tags: kekgaugetswe/webapi:latest
        
    
      
      
    - name: image deigest
      run: echo ${{ steps.build.outputs.diegest }}
      
    - name: testing doosy-deploy ssh  connection
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST}}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        script: |
        
             docker pull kekgaugetswe/webapi:latest;
             docker create --name webapi  -p 5089:80 kekgaugetswe/webapi:latest;
             docker start webapi;  
         
             docker ps -a
